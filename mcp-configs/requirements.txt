# DDN AI Test Failure Analysis - Python Dependencies
# Updated for Phase 0/1 - Install with: pip install -r requirements.txt
#
# IMPORTANT: For production, use requirements-lock.txt for reproducible builds
# See DEPENDENCY-MANAGEMENT-GUIDE.md for version management best practices

# Core Framework
flask==3.0.3
flask-cors==4.0.0
python-dotenv==1.0.1

# Caching (Phase 1)
redis>=5.0.1,<8.0.0

# CRITICAL: numpy 2.x breaks compatibility - stay on 1.24.x
# See: https://numpy.org/devdocs/numpy_2_0_migration_guide.html
numpy>=1.24.0,<1.25.0
scipy>=1.11.0,<1.12.0

# NLP & PII Detection (Phase 4)
# Pinned to avoid compatibility issues with numpy
spacy==3.7.5
thinc==8.2.5
presidio-analyzer>=2.2.354,<3.0.0
presidio-anonymizer>=2.2.354,<3.0.0

# Advanced Retrieval (Phase 2/3)
rank-bm25==0.2.2
sentence-transformers>=2.2.2,<6.0.0

# Async Task Processing (Phase 7)
celery==5.3.4
flower==2.0.1

# Monitoring & Tracing (Phase 8)
langfuse>=2.0.0
# langsmith>=0.4.39  # DISABLED: Conflicts with langchain dependencies. Using Langfuse instead for LLM observability

# Scheduling (Phase 0F & Phase 3)
APScheduler==3.10.4
schedule==1.2.0

# LangChain & LangGraph
langgraph==0.2.45
langchain==0.3.13
langchain-anthropic==0.3.5
langchain-openai==0.2.14
langchain-pinecone==0.2.0
langchain-community==0.3.13

# AI & ML
anthropic>=0.41.0
openai>=1.58.1

# Vector Database
# Changed from pinecone-client to pinecone (package renamed - see Phase 3-DEP)
pinecone>=5.0.0

# Databases
pymongo==4.10.1
psycopg2-binary==2.9.10

# HTTP & Requests
requests==2.32.3
urllib3==2.2.3

# Data Processing
pydantic==2.10.4
pydantic-settings==2.6.1

# Utilities
python-dateutil==2.9.0
pytz==2024.2

# Testing (Optional)
pytest==8.3.4
pytest-asyncio==0.24.0

# Logging
colorlog==6.9.0

# Production (Optional)
gunicorn==23.0.0
gevent==24.11.1
