# MongoDB Atlas Configuration
# Replace with your actual MongoDB Atlas connection string

# IMPORTANT: Replace these values with your actual MongoDB Atlas credentials
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://sushrutnistane097_db_user:Sharu%40051220@ddn-cluster.wudcfln.mongodb.net/ddn_tests?retryWrites=true&w=majority

# MongoDB Database and Collections
MONGODB_DATABASE=ddn_tests
MONGODB_COLLECTION_FAILURES=test_failures
MONGODB_COLLECTION_ANALYSIS=analysis_results

# Test Configuration
TEST_TIMEOUT=30000

# Jenkins URL (for local reference)
JENKINS_URL=http://localhost:8081

# DDN Endpoints (configure when ready)
DDN_EXASCALER_ENDPOINT=http://exascaler.ddn.local:8080
DDN_API_KEY=your-api-key-here
