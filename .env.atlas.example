# DDN AI Test Failure Analysis System - MongoDB Atlas Configuration
# Copy this to .env and fill in your MongoDB Atlas credentials

# ===================== MongoDB Atlas (Cloud Database) =====================

# Get your connection string from MongoDB Atlas:
# 1. Login to https://cloud.mongodb.com/
# 2. Click "Database" → "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
# 5. Replace <password> with your actual password

# IMPORTANT: URL-encode special characters in password
# @ → %40, # → %23, ! → %21, $ → %24, & → %26

# Example:
# Password: MyPass@123!
# Encoded: MyPass%40123%21
# Full: mongodb+srv://ddn_admin:MyPass%40123%21@cluster.mongodb.net/

MONGODB_URI=mongodb+srv://ddn_admin:YOUR_PASSWORD@ddn-ai-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=jenkins_failure_analysis

# ===================== AI & ML APIs =====================
ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX_NAME=test-failures

# ===================== GitHub =====================
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_REPO=your-org/your-repo

# ===================== Jira (Optional) =====================
JIRA_URL=https://your-company.atlassian.net
JIRA_EMAIL=your-email@company.com
JIRA_API_TOKEN=your-jira-api-token
JIRA_PROJECT_KEY=DDN

# ===================== Slack (Optional) =====================
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_DEFAULT_CHANNEL=#test-failures

# ===================== Jenkins (Optional) =====================
JENKINS_URL=http://localhost:8080
JENKINS_USER=admin
JENKINS_TOKEN=your-jenkins-api-token

# ===================== Self-Healing =====================
SELF_HEALING_SAFE_MODE=true
MIN_SUCCESS_RATE=0.8
MIN_PATTERN_OCCURRENCES=3

# ===================== Notes =====================
# - Keep .env file private (already in .gitignore)
# - Never commit credentials to Git
# - Rotate API keys regularly
# - Use strong passwords for MongoDB users
